# Server Configuration
PORT=7099

# Database Configuration (Optional)
# If database is not available, data will be forwarded to .NET API instead
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=smart_washer

# .NET API Forwarding (Optional)
# Set to false to disable .NET API forwarding
DOTNET_API_URL=https://localhost:7099/api/Data/insert-update
FORWARD_TO_DOTNET=false
